---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Lupa Password - ASTRO" description="Reset password akun ASTRO Anda">
  <div class="min-h-screen flex items-center justify-center p-6">
    <div class="w-full max-w-md space-y-8">
      <!-- Back Link -->
      <a href="/masuk" class="inline-flex items-center gap-2 text-secondary-400 hover:text-white transition-colors">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
        </svg>
        Kembali ke Login
      </a>

      <!-- Header -->
      <div class="text-center">
        <div class="w-16 h-16 mx-auto mb-6 rounded-2xl bg-gradient-primary flex items-center justify-center shadow-glow">
          <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 7a2 2 0 012 2m4 0a6 6 0 01-7.743 5.743L11 17H9v2H7v2H4a1 1 0 01-1-1v-2.586a1 1 0 01.293-.707l5.964-5.964A6 6 0 1121 9z"/>
          </svg>
        </div>
        <h1 class="text-2xl md:text-3xl font-display font-bold text-white mb-2">
          Lupa Password?
        </h1>
        <p class="text-secondary-400">
          Jangan khawatir! Masukkan email yang terdaftar dan kami akan mengirimkan link untuk reset password.
        </p>
      </div>

      <!-- Reset Form -->
      <form id="reset-form" class="space-y-6">
        <!-- Email -->
        <div>
          <label for="email" class="block text-sm font-medium text-secondary-300 mb-2">
            Email
          </label>
          <div class="relative">
            <input 
              type="email" 
              id="email" 
              name="email"
              required
              placeholder="nama@email.com"
              class="input pl-12"
            />
            <svg class="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-secondary-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
            </svg>
          </div>
        </div>

        <!-- Submit Button -->
        <button type="submit" class="btn-primary w-full py-4 text-lg">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
          </svg>
          Kirim Link Reset
        </button>
      </form>

      <!-- Success Message (Hidden by default) -->
      <div id="success-message" class="hidden text-center space-y-4">
        <div class="w-16 h-16 mx-auto rounded-full bg-green-500/20 flex items-center justify-center">
          <svg class="w-8 h-8 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
          </svg>
        </div>
        <h2 class="text-xl font-bold text-white">Email Terkirim!</h2>
        <p class="text-secondary-400">
          Kami telah mengirimkan link reset password ke email Anda. Silakan cek inbox atau folder spam.
        </p>
        <p class="text-sm text-secondary-500">
          Tidak menerima email? 
          <button id="resend-btn" class="text-primary-400 hover:text-primary-300">Kirim ulang</button>
        </p>
      </div>

      <!-- Help Text -->
      <div class="card p-4">
        <div class="flex items-start gap-3">
          <div class="w-8 h-8 rounded-lg bg-primary-500/20 flex items-center justify-center flex-shrink-0">
            <svg class="w-4 h-4 text-primary-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
          </div>
          <div>
            <h3 class="text-sm font-medium text-white mb-1">Butuh bantuan?</h3>
            <p class="text-xs text-secondary-400">
              Jika Anda mengalami masalah, silakan <a href="/kontak" class="text-primary-400 hover:text-primary-300">hubungi kami</a>.
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</BaseLayout>

<script>
  const resetForm = document.getElementById('reset-form');
  const successMessage = document.getElementById('success-message');

  resetForm?.addEventListener('submit', async (e) => {
    e.preventDefault();
    
    const formData = new FormData(e.target as HTMLFormElement);
    const email = formData.get('email');
    
    // TODO: Implement actual password reset
    console.log('Reset password for:', email);
    
    // Show success message
    resetForm.classList.add('hidden');
    successMessage?.classList.remove('hidden');
  });

  // Resend button
  const resendBtn = document.getElementById('resend-btn');
  resendBtn?.addEventListener('click', () => {
    console.log('Resend reset email');
    // TODO: Implement resend functionality
  });
</script>
